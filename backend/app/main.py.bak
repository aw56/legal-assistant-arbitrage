from fastapi import FastAPI
from backend.app.routes import decisions, analyze  # Импорт маршрутов
from backend.app.database import get_db  # Функция для получения сессии БД

app = FastAPI(
    title="Legal Assistant API",
    description="Арбитражный помощник: анализ, прогноз, фильтрация",
    version="0.1.0"
)

# Подключаем маршруты
app.include_router(decisions.router)
app.include_router(analyze.router)

@app.get("/")
def read_root():
    """
    Корневой эндпоинт. Проверка работы API.
    """
    return {"message": "Legal Assistant API is running"}
